{
  "name": "lark-slack-oauth-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker for Lark-Slack OAuth proxy",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
